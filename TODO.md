# To-Do

## Source

- Add non-scalar noise covariance factors in gp/kernel
    - Currently only have isotropic scalar noise in Kronecker-structure kernels
    - Want to at least support per-channel noise
- Triage `GaussianProcess` implementation issues found during docstring review
    - Define the return-shape contract for `prior_realization` and
        `posterior_realization`, then fix multi-output (`cdim > 1`) sampling;
        the `(n, cdim)` mean is currently added directly to an
        `(n * cdim, p)` random term, causing errors or incorrect broadcasting.
    - Add consistent conditioned-state checks to `tune`, `posterior_mean`,
        `posterior_covariance`, and `posterior_realization`; these currently fail
        with missing-attribute errors when called before `condition`.
    - Clarify or fix one-dimensional `get_sample_point` domain handling: a
        `hull` satisfies the initial exclusive-or check but is subsequently
        rejected because one-dimensional sampling requires `ranges`.

## Tests

- Write tests

## Docs

- Reorganize Tutorials
- Rewrite/improve tutorials:
    - template.md
    - (search for others)
- Possibly rework autogenerated doc signatures
    - Full signature is very hard to read
    - Maybe just print arg names in signature and doc arg types/definitions in
        body/item docstring.

## Today
- Docs in:
    - UncertainSCI.gp (top-level module)
    - UncertainSCI.gp.kernel
    - UncertainSCI.mean
- 2-d example notebook
- Tests (optional)
- Project/UncertainSCI docs
    - Add notebooks to docs
    - Finish formalism
        - Computational section
        - Notes on GP emulators
            - See Rasmussen & Williams for motivation
            - Add Rasmussen & Williams as project reference
    - (DO NOT) rework Tutorials docs
        - Needs its own PR/commit(s)
